{
  "name": "@hotel-copy/shared",
  "version": "0.0.1",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "tsc --noEmit",
    "dev": "tsc --noEmit --watch"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
